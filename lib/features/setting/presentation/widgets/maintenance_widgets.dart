import 'package:flutter/material.dart';

class SelectDeviceNumberWidget extends StatelessWidget {
  const SelectDeviceNumberWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
